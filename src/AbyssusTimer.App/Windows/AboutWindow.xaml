<Window x:Class="AbyssusTimer.App.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Abyssplit"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False">
    <Border Background="#17211D" BorderBrush="#3A4A44" BorderThickness="1" CornerRadius="8" Padding="24" MinWidth="320" MaxWidth="380">
        <StackPanel>
            <Image Source="/Assets/AppIcon.ico" Width="56" Height="56" HorizontalAlignment="Center" />
            <TextBlock Text="ABYSSPLIT" Foreground="#C98F3F" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,12,0,0" />
            <TextBlock x:Name="VersionText" Foreground="#8A9994" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,2,0,0" />
            <TextBlock Text="A speedrun timer for Abyssus, reading run state directly out of the game's memory."
                       Foreground="#ECE3D0" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" Margin="0,16,0,0" />
            <Button x:Name="GitHubButton" Content="View on GitHub" Cursor="Hand"
                    Padding="14,8" FontSize="12" Margin="0,18,0,0" HorizontalAlignment="Center"
                    Background="#17211D" Foreground="#ECE3D0" BorderBrush="#3A4A44" BorderThickness="1"
                    Click="GitHub_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1F2A26" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="TermsButton" Content="View Terms of Use" Cursor="Hand"
                    Padding="14,8" FontSize="12" Margin="0,8,0,0" HorizontalAlignment="Center"
                    Background="#17211D" Foreground="#8A9994" BorderBrush="#3A4A44" BorderThickness="1"
                    Click="Terms_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1F2A26" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="CloseButton" Content="Close" Cursor="Hand"
                    Padding="14,8" FontSize="12" Margin="0,8,0,0" HorizontalAlignment="Center"
                    Background="#17211D" Foreground="#8A9994" BorderBrush="#3A4A44" BorderThickness="1"
                    Click="Close_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1F2A26" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</Window>
